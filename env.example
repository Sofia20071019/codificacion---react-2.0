# Copia este archivo a ".env" (mismo nombre, sin ".example") antes de levantar
# los contenedores. NUNCA subas el .env real a GitHub (ya debe estar en .gitignore).

# --- MySQL ---
MYSQL_ROOT_PASSWORD=raiz_super_segura
MYSQL_DATABASE=Kimuka3_db
MYSQL_USER=kimuka_user
MYSQL_PASSWORD=kimuka_pass

# --- Backend Flask ---
SECRET_KEY=cambia-esto-por-una-clave-larga-y-aleatoria

# --- Frontend ---
# En Codespaces, después de levantar los contenedores, cambia esto por la URL
# pública que Codespaces te da para el puerto 5000 (ver paso 6 de la guía).
VITE_API_URL=http://localhost:5000
